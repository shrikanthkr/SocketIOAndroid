<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="socket.shriku.com.socketandroid"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name="socket.shriku.com.SocketApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/MyTheme" >

        <activity
            android:name="socket.shriku.com.activities.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="socket.shriku.com.activities.SigninActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name="socket.shriku.com.activities.SignupActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="socket.shriku.com.activities.IndexActivity"
            android:label="@string/app_name">

        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="0d49e5d4028eeb9be6fba4c81c0e1f93c8731b88" />
    </application>

</manifest>
